cradle: 
  # cabal: 
  #   - path: "."
  #     component: "lib:wtp"
  #   - path: "."
  #     component: "exe:wtp"
  #   - path: "."
  #     component: "test:wtp-test"
  direct: 
    arguments:
      - -Wall
      - -ilib
      - -itest
      - -iwtp
      - wtp/Main.hs
      - test/Main.hs
      - -idist/build/autogen

