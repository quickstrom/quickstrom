module Main where

import Prelude

import WebCheck.Web.Editor as Editor
import Effect (Effect)
import Halogen.Aff as HA
import Halogen.VDom.Driver (runUI)

main :: Effect Unit
main = HA.runHalogenAff do
  body <- HA.awaitBody
  runUI Editor.component unit body
